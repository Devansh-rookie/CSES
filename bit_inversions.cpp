#include<bits/stdc++.h>
using namespace std;
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    string s;
    int m = 0;
    cin >> s;
    int n = s.length();
    cin >> m;
    vector<int> changes(m, 0);

    for(int i = 0;i < m;i++) cin >> changes[i];

    

    return 0;
}